#include <bits/stdc++.h>
#define ll long long int
#define ld long double
const int MOD = 1e9;
using namespace std;

void solve(){
    int n, p1, p2, p3, t1, t2; 
    int in, in2;
    scanf("%d %d %d %d %d %d %d", n, p1, p2, p3, t1, t2);
    for (int i = 0; i < n; i++) {
        scanf("%d", in);
        scanf("%d", in2);
        
    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();

    return 0;
}   