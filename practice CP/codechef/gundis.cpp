#include <bits/stdc++.h>
#define ll long long

using namespace std;
const int MOD = 1000000007;

void solve(){
    // N guns 
    // A  
    // 1 buyer at a time, eksklusif 1 pembeli
    // hanya akan akan menjual jika senjata sebelumnya laku
    // 
    int t; cin >> t;
    for (int i=0;i<t;++i) {
        int n; cin >> n;
        int arrN[n];
        for (int j=0;j<n;++j) {
            cin >> arrN[i];
        }

    }

}

int main(){
    ios_base::sync_with_stdio(0); 
    cin.tie(0); 

    solve();

    return 0;
}   