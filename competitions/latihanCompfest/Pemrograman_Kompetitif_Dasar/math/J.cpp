#include <bits/stdc++.h>

using namespace std;

int main (){
    int n, m;
    cin >> n >> m;
    int x[n];

    // for (int i = 0; i < n; i++)
    // {
    //     cin >> x[i];
    // }

    cout << abs(-2 - 8);
    return 0;
}

